{
  "name": "backend",
  "version": "1.0.0",
  "main": "scrapper.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "apt-get update && apt-get install -y wget --no-install-recommends && rm -rf /var/lib/apt/lists/*"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tensorflow-models/coco-ssd": "^2.2.3",
    "@tensorflow/tfjs": "^4.22.0",
    "canvas": "^2.11.2",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^23.7.1",
    "puppeteer-core": "^23.7.1",
    "tesseract.js": "^5.1.1"
  }
}
